# Ostium TypeScript SDK

Early-stage TypeScript SDK for interacting with the Ostium trading platform. The structure mirrors the Python SDK and provides a starting point for JavaScript/TypeScript projects.

Run `bun run build` to compile the source.
